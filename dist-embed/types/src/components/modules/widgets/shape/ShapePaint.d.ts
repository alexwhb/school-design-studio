type Props = {
    params: Record<string, any>;
    /** Any CSS `border-radius`: four lengths for a box, `50%` for an ellipse. */
    radius: string;
};
export default function ShapePaint({ params, radius }: Props): import("react").JSX.Element;
export {};
