import './imageBackground.less';
type Props = {
    uuid: string;
    imgUrl?: string;
    originalImgUrl?: string;
};
export default function ImageBackground({ uuid, imgUrl, originalImgUrl }: Props): import("react").JSX.Element | null;
export {};
