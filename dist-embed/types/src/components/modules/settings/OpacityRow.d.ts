import './opacityRow.less';
type Props = {
    value?: number;
    onChange: (value: number) => void;
};
export default function OpacityRow({ value, onChange }: Props): import("react").JSX.Element;
export {};
