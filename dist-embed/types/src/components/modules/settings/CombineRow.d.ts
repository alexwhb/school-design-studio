import './combineRow.less';
type Props = {
    /** Something in the selection is not a shape, so there is nothing to combine. */
    disabled?: boolean;
};
export default function CombineRow({ disabled }: Props): import("react").JSX.Element;
export {};
